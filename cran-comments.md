## Resubmission notes
Thank you for the feedback! This is a resubmission:

1.  **DESCRIPTION References:** Formatted references in the DESCRIPTION file to match the required format (e.g., `Authors (year) <doi:...>`), ensuring no spaces after `doi:`.

2.  **Missing \value tags:** Added the missing `\value` tags to the exported print methods (`print.coxphMixture.Rd` and `print.glmMixture.Rd`), explicitly documenting that they invisibly return the input object.

3.  **Example structuring:** Removed `\dontrun{}` tags. Fast-executing examples (< 5s) have been fully unwrapped for routine testing. Long-running Bayesian/MCMC examples have been wrapped in `\donttest{}`.

## R CMD check results
0 errors | 0 warnings | 3 notes

This is a new release.

### Explanations for NOTEs

* **checking CRAN incoming feasibility ... NOTE**
  Maintainer: 'Priyanjali Bukke <postlink.group@gmail.com>'
  New submission
  
  *Explanation:* Thank you, this information is accurate!

* **checking installed package size ... NOTE**

  *Explanation:* The installed package size exceeds 5MB. This is expected behavior, as the `libs/` directory contains pre-compiled C++ binaries generated by `rstantools` and `rstan`. The source tarball remains under the 5MB CRAN limit.

* **checking compilation flags used ... NOTE**
  Compilation used the following non-portable flag(s): '-Wa,-mbig-obj'
  
  *Explanation:* This flag is strictly confined to `src/Makevars.win` and is required to prevent the Windows GCC assembler from failing with a "too many sections" error when compiling heavy Stan C++ templates.

* **Possibly misspelled words in DESCRIPTION:** PLDA (22:84)
  unlinked (22:316)
  
  *Explanation:* Thank you for this check! These are spelled as intended. "PLDA" is an acronym used to refer to "post-linkage data analysis" (as stated in the description: "Provides a suite of statistical tools for post-linkage data analysis (PLDA)."). "unlinked" is a standard and intentional statistical term in this specific domain.
