% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixbayes_survreg_methods.R
\name{confint.survMixBayes}
\alias{confint.survMixBayes}
\title{Confidence intervals for survMixBayes parameters}
\usage{
\method{confint}{survMixBayes}(object, parm = NULL, level = 0.95, ...)
}
\arguments{
\item{object}{An object of class \code{survMixBayes}.}

\item{parm}{Optional. Character vector selecting which elements of the returned
list to keep. If \code{NULL}, all intervals are returned.}

\item{level}{Confidence level.}

\item{...}{Additional arguments (unused).}
}
\value{
A named list of credible intervals. Elements \code{coef1} and \code{coef2}
are \verb{p x 2} matrices (lower/upper) for component-specific regression
coefficients when available. Elements \code{theta}, \code{shape1}, \code{shape2},
\code{scale1}, \code{scale2} are numeric vectors of length 2 giving lower/upper
credible intervals for scalar parameters.
}
\description{
Returns posterior credible intervals for regression coefficients and key parameters.
}
\examples{
\donttest{
# Simulate data
set.seed(403)
n <- 100
X <- matrix(rnorm(n * 2), ncol = 2, dimnames = list(NULL, c("x1", "x2")))
y <- cbind(time = rweibull(n, shape = 1.2, scale = exp(X[, 1])),
           event = rbinom(n, 1, 0.8))

# Fit the model
fit <- survregMixBayes(
  X = X, y = y, dist = "weibull",
  control = list(iterations = 100, burnin.iterations = 50, seed = 403)
)

# Calculate 95\% credible intervals for all parameters
confint(fit, level = 0.95)

# Extract credible intervals specifically for the mixing weight
confint(fit, parm = "theta", level = 0.90)
}

}
